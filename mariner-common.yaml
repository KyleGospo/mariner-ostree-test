variables:
  variant: "Mariner"

include:
  - common.yaml
  - baremetal-packages.yaml
  - base-image-packages.yaml
  - cloud-init-packages.yaml
  - selinux.yaml

ref: kylegospo/azure/${basearch}/mariner
rojig:
  name: mariner
  summary: "Azure Linux Atomic"
  license: MIT

packages:
  - mariner-release
